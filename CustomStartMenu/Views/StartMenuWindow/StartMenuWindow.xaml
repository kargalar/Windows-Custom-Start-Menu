<Window x:Class="CustomStartMenu.Views.StartMenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Custom Start Menu"
        Width="650"
        Height="750"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="True"
        Topmost="True"
        ResizeMode="NoResize"
        Loaded="Window_Loaded"
        Deactivated="Window_Deactivated"
        PreviewKeyDown="Window_PreviewKeyDown"
        PreviewTextInput="Window_PreviewTextInput"
        PreviewMouseWheel="Window_PreviewMouseWheel">

    <Window.Resources>
        <!-- Button Style -->
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#30FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#50FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Tab Button Style -->
        <Style x:Key="TabButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6,6,0,0"
                                Padding="{TemplateBinding Padding}"
                                BorderThickness="0,0,0,2"
                                BorderBrush="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#20FFFFFF"/>
                            </Trigger>
                            <Trigger Property="Tag" Value="Active">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                                <Setter TargetName="border" Property="Background" Value="#15FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Add Tab Button Style -->
        <Style x:Key="AddTabButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="ToolTip" Value="Yeni Sekme Ekle"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="4">
                            <TextBlock Text="+" FontSize="18" FontWeight="Bold" 
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#30FFFFFF"/>
                                <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Pinned Item Button Style -->
        <Style x:Key="PinnedItemStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="100"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="AllowDrop" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="8"
                                BorderThickness="2"
                                BorderBrush="Transparent">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#30FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#50FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Pinned Item Button Style - Icon Only Mode (60x60) -->
        <Style x:Key="PinnedItemIconOnlyStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="60"/>
            <Setter Property="Height" Value="60"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="AllowDrop" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="4"
                                BorderThickness="2"
                                BorderBrush="Transparent">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#30FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#50FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Group Header Style -->
        <Style x:Key="GroupHeaderStyle" TargetType="Border">
            <Setter Property="Background" Value="#15FFFFFF"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="0,8,0,4"/>
            <Setter Property="Padding" Value="12,8"/>
        </Style>

        <!-- Search Result Item Style -->
        <Style x:Key="SearchResultStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#30FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#50FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Search Result Item Style - Selected (keyboard navigation) -->
        <Style x:Key="SearchResultSelectedStyle" TargetType="Button">
            <Setter Property="Background" Value="#40FFFFFF"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}"
                                BorderThickness="2"
                                BorderBrush="{DynamicResource AccentBrush}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#50FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#60FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Search Box Style -->
        <Style x:Key="SearchBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#30FFFFFF"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="#50FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost" 
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"/>
                                <TextBlock x:Name="placeholder"
                                           Text="Aramak için yazmaya başlayın..."
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           Margin="{TemplateBinding Padding}"
                                           VerticalAlignment="Center"
                                           IsHitTestVisible="False"
                                           Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Main Container with animation -->
    <Border x:Name="MainBorder"
            Background="{StaticResource MenuBackgroundBrush}"
            BorderBrush="Transparent"
            BorderThickness="0"
            CornerRadius="8"
            Margin="8">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.5" Color="Black"/>
        </Border.Effect>

        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header with Tabs -->
            <Grid Grid.Row="0" Margin="0,0,0,8">
                <!-- Tab Bar (shown in normal view) -->
                <Grid x:Name="TabBarPanel">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Tabs -->
                    <ScrollViewer Grid.Column="0" 
                                  HorizontalScrollBarVisibility="Auto" 
                                  VerticalScrollBarVisibility="Disabled"
                                  Padding="0,0,0,4">
                        <StackPanel x:Name="TabsPanel" Orientation="Horizontal"/>
                    </ScrollViewer>

                    <!-- Add Tab Button -->
                    <Button x:Name="AddTabButton"
                            Grid.Column="1" 
                            Style="{StaticResource AddTabButtonStyle}"
                            Click="AddTab_Click"
                            Margin="8,0,0,0"/>
                </Grid>

                <!-- Close Button Header (shown in settings/search view) -->
                <Grid x:Name="CloseButtonHeader" Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock x:Name="ViewTitleText"
                               Grid.Column="0"
                               Text="Ayarlar"
                               FontSize="20"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextBrush}"
                               VerticalAlignment="Center"/>
                    
                    <Button x:Name="CloseViewButton"
                            Grid.Column="1"
                            Style="{StaticResource AddTabButtonStyle}"
                            Click="CloseViewButton_Click"
                            ToolTip="Kapat">
                        <TextBlock Text="✕" FontSize="14" FontWeight="Bold"/>
                    </Button>
                </Grid>

                <!-- Tab separator line -->
                <Border Height="1" Background="#30FFFFFF" 
                        VerticalAlignment="Bottom" Margin="0,0,0,0"/>
            </Grid>

            <!-- Content Area - Pinned Items View (Default) -->
            <Grid x:Name="PinnedItemsView" Grid.Row="1">
                <!-- Empty State -->
                <StackPanel x:Name="EmptyState" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="📌"
                               FontSize="48"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,12"/>
                    <TextBlock Text="Henüz pinlenmiş öğe yok"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,8"/>
                    <TextBlock Text="Bir exe veya klasöre sağ tıklayıp 'Custom Start Menu'ye Pinle' seçeneğini kullanın"
                               FontSize="13"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center"
                               TextWrapping="Wrap"
                               TextAlignment="Center"
                               MaxWidth="350"/>
                </StackPanel>

                <!-- Pinned Items with Groups and Pagination -->
                <Grid x:Name="PinnedScrollViewer">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Items Container -->
                    <StackPanel x:Name="PinnedItemsContainer" Grid.Row="0"/>
                    
                    <!-- Pagination Controls -->
                    <Grid x:Name="PaginationPanel" Grid.Row="1" Visibility="Collapsed" Margin="0,8,0,0">
                        <Button x:Name="PrevPageButton" 
                                Content="←" 
                                HorizontalAlignment="Left"
                                VerticalAlignment="Bottom"
                                Width="40" Height="40"
                                FontSize="20"
                                FontWeight="Bold"
                                Click="PrevPageButton_Click"
                                Style="{StaticResource MenuButtonStyle}"
                                Visibility="Collapsed"/>
                        <Button x:Name="NextPageButton" 
                                Content="→" 
                                HorizontalAlignment="Right"
                                VerticalAlignment="Bottom"
                                Width="40" Height="40"
                                FontSize="20"
                                FontWeight="Bold"
                                Click="NextPageButton_Click"
                                Style="{StaticResource MenuButtonStyle}"
                                Visibility="Collapsed"/>
                    </Grid>
                </Grid>
            </Grid>

            <!-- Content Area - Search View (Hidden by default) -->
            <Grid x:Name="SearchView" Grid.Row="1" Visibility="Collapsed">
                <!-- Search Box at top -->
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBox x:Name="SearchBox"
                         Grid.Row="0"
                         Style="{StaticResource SearchBoxStyle}"
                         TextChanged="SearchBox_TextChanged"
                         PreviewKeyDown="SearchBox_PreviewKeyDown"
                         Margin="0,0,0,16"/>

                <!-- Search Results -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Searching indicator -->
                        <TextBlock x:Name="SearchingText"
                                   Text="Aranıyor..."
                                   FontSize="14"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="12,8"
                                   Visibility="Collapsed"/>

                        <!-- No results -->
                        <TextBlock x:Name="NoResultsText"
                                   Text="Sonuç bulunamadı"
                                   FontSize="14"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   Margin="12,8"
                                   Visibility="Collapsed"/>

                        <!-- Results list -->
                        <ItemsControl x:Name="SearchResultsPanel">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </Grid>

            <!-- Content Area - Settings Page (Hidden by default) -->
            <Grid x:Name="SettingsView" Grid.Row="1" Visibility="Collapsed">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="0,0,0,16">
                        <!-- Settings Header with Back Button -->
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <Button x:Name="SettingsBackButton"
                                    Grid.Column="0"
                                    Style="{StaticResource MenuButtonStyle}"
                                    Click="SettingsBackButton_Click"
                                    ToolTip="Geri"
                                    Margin="0,0,12,0">
                                <TextBlock Text="←" FontSize="20"/>
                            </Button>
                            
                            <TextBlock Grid.Column="1"
                                       Text="Ayarlar"
                                       FontSize="20"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource TextBrush}"
                                       VerticalAlignment="Center"/>
                        </Grid>

                        <!-- Show Icons Only Toggle -->
                        <Border Background="#15FFFFFF" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Sadece simgeleri göster"
                                               FontSize="14"
                                               Foreground="{StaticResource TextBrush}"/>
                                    <TextBlock Text="Metin etiketlerini gizle"
                                               FontSize="12"
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               Margin="0,4,0,0"/>
                                </StackPanel>
                                
                                <CheckBox x:Name="IconsOnlyCheckBox"
                                          Grid.Column="1"
                                          Style="{StaticResource ToggleSwitchStyle}"
                                          VerticalAlignment="Center"
                                          Checked="IconsOnlyCheckBox_Changed"
                                          Unchecked="IconsOnlyCheckBox_Changed"/>
                            </Grid>
                        </Border>

                        <!-- Transparency Slider -->
                        <Border Background="#15FFFFFF" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock x:Name="TransparencyLabel"
                                           Text="Menü saydamlığı: 85%"
                                           FontSize="14"
                                           Foreground="{StaticResource TextBrush}"
                                           Margin="0,0,0,12"/>
                                <Slider x:Name="TransparencySlider"
                                        Minimum="0"
                                        Maximum="100"
                                        Value="85"
                                        ValueChanged="TransparencySlider_ValueChanged"/>
                            </StackPanel>
                        </Border>

                        <!-- Layout Mode -->
                        <Border Background="#15FFFFFF" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="Düzen modu"
                                           FontSize="14"
                                           Foreground="{StaticResource TextBrush}"
                                           Margin="0,0,0,12"/>
                                <ComboBox x:Name="LayoutModeComboBox"
                                          SelectionChanged="LayoutModeComboBox_SelectionChanged">
                                    <ComboBoxItem Content="Sıralı (Otomatik)" Tag="Ordered"/>
                                    <ComboBoxItem Content="Serbest (Manuel)" Tag="FreeForm"/>
                                </ComboBox>
                            </StackPanel>
                        </Border>

                        <!-- Web Search URL -->
                        <Border Background="#15FFFFFF" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="Web arama URL'si"
                                           FontSize="14"
                                           Foreground="{StaticResource TextBrush}"
                                           Margin="0,0,0,4"/>
                                <TextBlock Text="Yukarı ok tuşuyla web araması yaparken kullanılır"
                                           FontSize="12"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           Margin="0,0,0,12"/>
                                <TextBox x:Name="WebSearchUrlTextBox"
                                         Style="{StaticResource SearchBoxStyle}"
                                         LostFocus="WebSearchUrlTextBox_LostFocus"/>
                            </StackPanel>
                        </Border>

                        <!-- Separator -->
                        <Border Height="1" Background="#30FFFFFF" Margin="0,8,0,20"/>
                        <TextBlock Text="Görünüm ve Davranış"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextBrush}"
                                   Margin="0,0,0,16"/>

                        <!-- Enable Animations Toggle -->
                        <Border Background="#15FFFFFF" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Animasyonları etkinleştir"
                                               FontSize="14"
                                               Foreground="{StaticResource TextBrush}"/>
                                    <TextBlock Text="Menü açılış/kapanış animasyonları"
                                               FontSize="12"
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               Margin="0,4,0,0"/>
                                </StackPanel>
                                
                                <CheckBox x:Name="AnimationsCheckBox"
                                          Grid.Column="1"
                                          Style="{StaticResource ToggleSwitchStyle}"
                                          VerticalAlignment="Center"
                                          Checked="AnimationsCheckBox_Changed"
                                          Unchecked="AnimationsCheckBox_Changed"/>
                            </Grid>
                        </Border>

                        <!-- Menu Position -->
                        <Border Background="#15FFFFFF" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="Menü konumu"
                                           FontSize="14"
                                           Foreground="{StaticResource TextBrush}"
                                           Margin="0,0,0,12"/>
                                <ComboBox x:Name="PositionComboBox"
                                          SelectionChanged="PositionComboBox_SelectionChanged">
                                    <ComboBoxItem Content="Sol" Tag="Left"/>
                                    <ComboBoxItem Content="Orta" Tag="Center"/>
                                </ComboBox>
                            </StackPanel>
                        </Border>

                        <!-- Menu Size -->
                        <Border Background="#15FFFFFF" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="Menü boyutu"
                                           FontSize="14"
                                           Foreground="{StaticResource TextBrush}"
                                           Margin="0,0,0,12"/>
                                <ComboBox x:Name="SizeComboBox"
                                          SelectionChanged="SizeComboBox_SelectionChanged">
                                    <ComboBoxItem Content="Küçük (500x600)" Tag="Small"/>
                                    <ComboBoxItem Content="Normal (650x750)" Tag="Normal"/>
                                    <ComboBoxItem Content="Büyük (900x850)" Tag="Large"/>
                                    <ComboBoxItem Content="Çok Büyük (1100x950)" Tag="VeryLarge"/>
                                    <ComboBoxItem Content="Tam Ekran" Tag="Fullscreen"/>
                                    <ComboBoxItem Content="Özel" Tag="Custom"/>
                                </ComboBox>
                                
                                <!-- Custom Size Inputs -->
                                <StackPanel x:Name="CustomSizePanel" Visibility="Collapsed" Margin="0,12,0,0">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="16"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="Genişlik" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                                            <TextBox x:Name="CustomWidthTextBox" Style="{StaticResource SearchBoxStyle}" Text="650" LostFocus="CustomSizeTextBox_LostFocus"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="Yükseklik" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                                            <TextBox x:Name="CustomHeightTextBox" Style="{StaticResource SearchBoxStyle}" Text="750" LostFocus="CustomSizeTextBox_LostFocus"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Item Size -->
                        <Border Background="#15FFFFFF" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock x:Name="ItemSizeLabel"
                                           Text="Öğe boyutu: 80px"
                                           FontSize="14"
                                           Foreground="{StaticResource TextBrush}"
                                           Margin="0,0,0,12"/>
                                <Slider x:Name="ItemSizeSlider"
                                        Minimum="40"
                                        Maximum="150"
                                        Value="80"
                                        IsSnapToTickEnabled="True"
                                        TickFrequency="10"
                                        ValueChanged="ItemSizeSlider_ValueChanged"/>
                                <TextBlock Text="Öğe boyutu arttıkça satır başına öğe sayısı azalır"
                                           FontSize="11"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           Margin="0,8,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Separator -->
                        <Border Height="1" Background="#30FFFFFF" Margin="0,8,0,20"/>
                        <TextBlock Text="Kısayol Tuşu"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextBrush}"
                                   Margin="0,0,0,16"/>

                        <!-- Override Windows Start Button -->
                        <Border Background="#15FFFFFF" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Taskbar başlat butonunu kullan"
                                               FontSize="14"
                                               Foreground="{StaticResource TextBrush}"/>
                                    <TextBlock Text="Taskbardaki Start butonuna tıklayınca bu menü açılır"
                                               FontSize="12"
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               Margin="0,4,0,0"/>
                                </StackPanel>
                                
                                <CheckBox x:Name="OverrideWinKeyCheckBox"
                                          Grid.Column="1"
                                          Style="{StaticResource ToggleSwitchStyle}"
                                          VerticalAlignment="Center"
                                          Checked="OverrideWinKeyCheckBox_Changed"
                                          Unchecked="OverrideWinKeyCheckBox_Changed"/>
                            </Grid>
                        </Border>

                        <!-- Hotkey Configuration -->
                        <Border Background="#15FFFFFF" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="Kısayol tuşu kombinasyonu"
                                           FontSize="14"
                                           Foreground="{StaticResource TextBrush}"
                                           Margin="0,0,0,12"/>
                                
                                <Grid>
                                                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Current Hotkey Display -->
                                    <Border x:Name="HotkeyDisplayBorder" 
                                            Background="#10FFFFFF" 
                                            CornerRadius="6" 
                                            Padding="12,8" 
                                            Margin="0,0,12,0">
                                        <TextBlock x:Name="HotkeyDisplayText"
                                                   Text="Win"
                                                   FontSize="14"
                                                   Foreground="{StaticResource TextBrush}"
                                                   VerticalAlignment="Center"/>
                                    </Border>
                                    
                                    <!-- Assign Button -->
                                    <Button x:Name="HotkeyAssignButton" 
                                            Grid.Column="1"
                                            Content="Ata"
                                            Style="{StaticResource MenuButtonStyle}"
                                            Background="{DynamicResource AccentBrush}"
                                            Padding="16,8"
                                            Click="HotkeyAssignButton_Click"/>
                                    
                                    <!-- Save Button (hidden initially) -->
                                    <Button x:Name="HotkeySaveButton" 
                                            Grid.Column="2"
                                            Content="Kaydet"
                                            Style="{StaticResource MenuButtonStyle}"
                                            Background="#22C55E"
                                            Padding="16,8"
                                            Margin="8,0,0,0"
                                            Visibility="Collapsed"
                                            Click="HotkeySaveButton_Click"/>
                                    
                                    <!-- Cancel Button (hidden initially) -->
                                    <Button x:Name="HotkeyCancelButton" 
                                            Grid.Column="3"
                                            Content="İptal"
                                            Style="{StaticResource MenuButtonStyle}"
                                            Background="#EF4444"
                                            Padding="16,8"
                                            Margin="8,0,0,0"
                                            Visibility="Collapsed"
                                            Click="HotkeyCancelButton_Click"/>
                                </Grid>
                                
                                <!-- Instruction Text -->
                                <TextBlock x:Name="HotkeyInstructionText"
                                           Text=""
                                           FontSize="11"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           Margin="0,8,0,0"
                                           TextWrapping="Wrap"
                                           Visibility="Collapsed"/>
                            </StackPanel>
                        </Border>

                        <!-- Separator -->
                        <Border Height="1" Background="#30FFFFFF" Margin="0,8,0,20"/>
                        <TextBlock Text="Tema"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextBrush}"
                                   Margin="0,0,0,16"/>

                        <!-- Theme Color -->
                        <Border Background="#15FFFFFF" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock Text="Tema rengi"
                                           FontSize="14"
                                           Foreground="{StaticResource TextBrush}"
                                           Margin="0,0,0,12"/>
                                <WrapPanel>
                                    <Button x:Name="ColorBlue" Style="{StaticResource MenuButtonStyle}" Background="#0078D4" Width="36" Height="36" Margin="0,0,8,8" Click="ThemeColor_Click" Tag="#0078D4" ToolTip="Mavi"/>
                                    <Button x:Name="ColorPurple" Style="{StaticResource MenuButtonStyle}" Background="#8B5CF6" Width="36" Height="36" Margin="0,0,8,8" Click="ThemeColor_Click" Tag="#8B5CF6" ToolTip="Mor"/>
                                    <Button x:Name="ColorPink" Style="{StaticResource MenuButtonStyle}" Background="#EC4899" Width="36" Height="36" Margin="0,0,8,8" Click="ThemeColor_Click" Tag="#EC4899" ToolTip="Pembe"/>
                                    <Button x:Name="ColorRed" Style="{StaticResource MenuButtonStyle}" Background="#EF4444" Width="36" Height="36" Margin="0,0,8,8" Click="ThemeColor_Click" Tag="#EF4444" ToolTip="Kırmızı"/>
                                    <Button x:Name="ColorOrange" Style="{StaticResource MenuButtonStyle}" Background="#F97316" Width="36" Height="36" Margin="0,0,8,8" Click="ThemeColor_Click" Tag="#F97316" ToolTip="Turuncu"/>
                                    <Button x:Name="ColorYellow" Style="{StaticResource MenuButtonStyle}" Background="#EAB308" Width="36" Height="36" Margin="0,0,8,8" Click="ThemeColor_Click" Tag="#EAB308" ToolTip="Sarı"/>
                                    <Button x:Name="ColorGreen" Style="{StaticResource MenuButtonStyle}" Background="#22C55E" Width="36" Height="36" Margin="0,0,8,8" Click="ThemeColor_Click" Tag="#22C55E" ToolTip="Yeşil"/>
                                    <Button x:Name="ColorTeal" Style="{StaticResource MenuButtonStyle}" Background="#14B8A6" Width="36" Height="36" Margin="0,0,8,8" Click="ThemeColor_Click" Tag="#14B8A6" ToolTip="Turkuaz"/>
                                    <Button x:Name="ColorCyan" Style="{StaticResource MenuButtonStyle}" Background="#06B6D4" Width="36" Height="36" Margin="0,0,8,8" Click="ThemeColor_Click" Tag="#06B6D4" ToolTip="Camgöbeği"/>
                                    <Button x:Name="ColorGray" Style="{StaticResource MenuButtonStyle}" Background="#6B7280" Width="36" Height="36" Margin="0,0,8,8" Click="ThemeColor_Click" Tag="#6B7280" ToolTip="Gri"/>
                                    <Button x:Name="ColorBlack" Style="{StaticResource MenuButtonStyle}" Background="#1F1F1F" Width="36" Height="36" Margin="0,0,8,8" Click="ThemeColor_Click" Tag="#1F1F1F" ToolTip="Siyah" BorderBrush="#404040" BorderThickness="1"/>
                                </WrapPanel>
                            </StackPanel>
                        </Border>

                        <!-- Background Darkness Slider -->
                        <Border Background="#15FFFFFF" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                            <StackPanel>
                                <TextBlock x:Name="BackgroundDarknessLabel"
                                           Text="Arka plan koyuluğu: 32"
                                           FontSize="14"
                                           Foreground="{StaticResource TextBrush}"
                                           Margin="0,0,0,12"/>
                                <Slider x:Name="BackgroundDarknessSlider"
                                        Minimum="0"
                                        Maximum="80"
                                        Value="32"
                                        ValueChanged="BackgroundDarknessSlider_ValueChanged"/>
                                <TextBlock Text="0 = Siyah, 80 = Açık gri"
                                           FontSize="11"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           Margin="0,8,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Separator -->
                        <Border Height="1" Background="#30FFFFFF" Margin="0,8,0,20"/>
                        <TextBlock Text="Veri Yönetimi"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextBrush}"
                                   Margin="0,0,0,16"/>

                        <!-- Clear All Pins -->
                        <Border Background="#15FFFFFF" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Tüm sabitlenmiş öğeleri temizle"
                                               FontSize="14"
                                               Foreground="{StaticResource TextBrush}"/>
                                    <TextBlock Text="Tüm sekmeler, gruplar ve sabitlenmiş uygulamalar silinir"
                                               FontSize="12"
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               Margin="0,4,0,0"/>
                                </StackPanel>
                                
                                <Button x:Name="ClearAllPinsButton"
                                        Grid.Column="1"
                                        Content="Temizle"
                                        Style="{StaticResource MenuButtonStyle}"
                                        Background="#EF4444"
                                        Padding="16,8"
                                        Click="ClearAllPinsButton_Click"/>
                            </Grid>
                        </Border>

                        <!-- Clear All Data -->
                        <Border Background="#15FFFFFF" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Tüm verileri temizle"
                                               FontSize="14"
                                               Foreground="{StaticResource TextBrush}"/>
                                    <TextBlock Text="Ayarlar dahil tüm veriler silinir ve uygulama sıfırlanır"
                                               FontSize="12"
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               Margin="0,4,0,0"/>
                                </StackPanel>
                                
                                <Button x:Name="ClearAllDataButton"
                                        Grid.Column="1"
                                        Content="Tümünü Sil"
                                        Style="{StaticResource MenuButtonStyle}"
                                        Background="#DC2626"
                                        Padding="16,8"
                                        Click="ClearAllDataButton_Click"/>
                            </Grid>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>

            <!-- Footer -->
            <Grid Grid.Row="2" Margin="0,16,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Settings Button (Icon Only) -->
                <Button Grid.Column="1"
                        Style="{StaticResource MenuButtonStyle}"
                        Click="SettingsButton_Click"
                        ToolTip="Ayarlar"
                        Margin="0,0,8,0">
                    <TextBlock Text="⚙" FontSize="20"/>
                </Button>

                <!-- Power Button (Icon Only) -->
                <Button Grid.Column="2"
                        Style="{StaticResource MenuButtonStyle}"
                        Click="PowerButton_Click"
                        ToolTip="Güç Seçenekleri">
                    <TextBlock Text="⏻" FontSize="20"/>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>
